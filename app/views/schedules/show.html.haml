= @schedule.first.start.strftime("%B %d, %A")
%span -
= @schedule.first.end.strftime("%B %d, %A")

%br
- @schedule.each do |s|
  = s.shift_id
  - @shift = Shift.where(:id => s.shift_id)
  = @shift[0].start
  = @shift[0].end
  = s.employee
  %br
